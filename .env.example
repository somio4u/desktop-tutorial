# Dify (self-hosted or cloud). Create a "Workflow" app in Dify with:
#   input variable:  prompt   (and optionally: genre)
#   output variable: story
# then publish it and copy the API key from the app's "API Access" page.
DIFY_API_BASE_URL=https://api.dify.ai/v1
DIFY_API_KEY=

# ElevenLabs
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

PORT=3000
